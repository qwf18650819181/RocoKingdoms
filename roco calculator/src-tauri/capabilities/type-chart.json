{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "type-chart-capability",
  "description": "Capability for the type chart floating window",
  "windows": ["type-chart"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:event:default"
  ]
}
